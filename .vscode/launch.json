{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "g++", // 또는 사용할 컴파일러
            "args": [
                "-g",
                "${file}", // 현재 파일
                "-o",
                "${fileDirname}/${fileBasenameNoExtension}" // 실행 파일 이름
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Generated task by g++."
        }
    ]
}
